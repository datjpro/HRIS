{
  "name": "@hris/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@hris/db": "workspace:*",
    "@hris/redis": "workspace:*",
    "@hris/shared-types": "workspace:*",
    "hono": "^4.7.8"
  }
}

