# Task Plan - Authentication & RBAC Implementation (Phase 1)

## Metadata
- Task: Thay the mock auth bang JWT + Refresh Token va RBAC dong.
- Branch goc: `dev`
- Branch lam viec: `feat/auth-jwt-implementation`
- Trang thai task: `IN PROGRESS`

## Muc tieu
- Trien khai dang nhap, dang ky, lam moi token dung JWT.
- Refactor middleware de xac thuc token that thay vi header gia.
- Chuan bi DB schema cho tai khoan va token.

## Phan viec

### P1 - Shared Types & DB Schema
- Status: `DONE`
- Success criteria:
  - `packages/shared-types` co `AuthPayload`, `JWTSchema`, `LoginDTO`.
  - `packages/db/prisma/schema.prisma` co model `User`, `Account`, `RefreshToken`.
- Files du kien tac dong:
  - `packages/shared-types/src/api.ts`
  - `packages/db/prisma/schema.prisma`
- Commit map:
  - `chore: update shared types and db schema for auth`

### P2 - Server Dependencies & Utils
- Status: `IN PROGRESS`
- Success criteria:
  - Cai dat `hono/jwt`, `bcrypt` (hoac `bun` password hashing).
  - Co `apps/server/src/lib/jwt.ts` helper.
- Files du kien tac dong:
  - `apps/server/package.json`
  - `apps/server/src/lib/jwt.ts`
- Commit map:
  - `chore: add auth dependencies and jwt utils`

### P3 - Auth Routes & Logic
- Status: `TODO`
- Success criteria:
  - API `/api/v1/auth/login` tra ve access + refresh token.
  - API `/api/v1/auth/register` tao user moi (Admin/HR only or public based on config).
  - API `/api/v1/auth/refresh` cap access token moi.
- Files du kien tac dong:
  - `apps/server/src/routes/auth.ts`
  - `apps/server/src/index.ts`
- Commit map:
  - `feat: implement auth routes with jwt logic`

### P4 - Middleware Refactoring
- Status: `TODO`
- Success criteria:
  - `authMiddleware` thuc hien verify JWT tu Authorization header.
  - `roleMiddleware` lay role tu JWT payload.
- Files du kien tac dong:
  - `apps/server/src/middlewares/auth.ts`
  - `apps/server/src/middlewares/role.ts`
- Commit map:
  - `feat: refactor middlewares to use real jwt verification`

### P5 - Validation & Tests
- Status: `TODO`
- Success criteria:
  - Chay build monorepo thanh cong.
  - Test login/refresh/protected route qua Postman/Curl thanh cong.
- Commit map:
  - `test: validate auth flow`

## Quy uoc trang thai
- `TODO`, `IN PROGRESS`, `DONE`, `BLOCKED`
